# stepFlow 次チャット用 引き継ぎプロンプト（全文）

**使い方:** 新チャットを開いたら、下の「■ 新チャットにそのまま貼る文」から最後まで **全部コピーして貼る**。
必要なら `document/チーム開発ロードマップ（お手本）.xlsx` 等も添付。

---

## ■ 新チャットにそのまま貼る文（ここから下を全部コピー）

---

あなた（AI）へのお願い：

- 私は **自分でコードを打つ** のが基本。まず **手順・模写コード（行ごとの日本語コメント）・レビュー** を出してほしい。
- 「修正して」「レビューして」と言ったときだけ、ローカルで直してよい。
- 仕様の正は **`document/` 配下**（チーム開発ロードマップお手本 Excel/PDF）。食い違いは Excel 優先。
- 既存コードの **書き方・インデント・コメント量** に合わせる（特に `ShopOrderController` / `ShopSalesController` / `ShopOrderService`）。

---

## プロジェクト

| 項目 | 値 |
|------|-----|
| ローカルパス | `C:\Users\owner\ForDevelop\stepflow\stepflow` |
| GitHub | https://github.com/kaedenakamura/stepFlow.git |
| 技術 | Spring Boot 3.4.3 / Java 17 / MySQL / JPA / Thymeleaf / Spring Security / Lombok / Apache POI 5.2.5 / OpenPDF 2.0.3 |
| DB | `stepflow_db`（`application.properties`） |
| ビルド | `.\scripts\dev-check.bat` または `.\mvnw.cmd clean compile` |
| 起動 | `.\scripts\run-app.bat` → http://localhost:8080 |
| 権限 | 店舗 `/shop/**` = ROLE_SHOP、倉庫 `/warehouse/**` = ROLE_WAREHOUSE |

---

## このチャットで完了したこと（店舗中心）

| 機能 | URL / ファイル | 状態 |
|------|----------------|------|
| 発注商品選択 | `GET /shop/order` | 済 |
| 発注1件登録 | `POST /shop/order` | 済 |
| ExcelテンプレDL（A3） | `GET /shop/order/template/download` | 済 |
| Excel一括発注（A4） | `GET/POST /shop/order/import` | 済（`ShopOrderExcelService.importOrdersFromExcel`） |
| 発注履歴・絞り込み | `GET /shop/sales` | 済 |
| CSVエクスポート | `GET /shop/sales/csv` | 済 |
| PDFエクスポート | `GET /shop/sales/pdf` | 済（`ShopSalesPdfService` + `fonts/ipaexg.ttf`） |
| 学習用バッチ | `scripts/*.bat` + `@Scheduled` デモユーザー追加 | 済（本番機能ではない） |
| Cursor Privacy Mode | Web / アプリ設定 | ユーザーが ON 済み |

**店舗のロードマップ上の必須（発注まわり）は一通り完了。** 次は **倉庫側**。

---

## 今やること（最優先・まだ模写していない）

### 倉庫：受注一覧の絞り込み

| 項目 | 内容 |
|------|------|
| URL | `GET /warehouse/stock` |
| 画面 | `templates/warehouse/stock-list.html` |
| 条件 | クエリ `status`（準備中/発注済）、`from`、`to`（日付） |
| 対象データ | ログイン倉庫 `user.warehouse_id` への `shop_order` のみ |
| 写し元（店舗・済） | `ShopOrderService.findActiveByShopIdFiltered` + `ShopSalesController` + `shop/sales-list.html` |

### 触るファイル（この順で模写）

1. `ShopOrderRepository.java` … warehouse 用 `findByWarehouseId...` メソッド **3つ**（店舗の `findByShopId...` と同型）
2. `WarehouseOrderService.java` … `findActiveByWarehouseIdFiltered` 追加
3. `WarehouseOrderController.java` … `list` に `@RequestParam status/from/to`、Service 呼び出し、Model に `statusFilter` `fromDate` `toDate`
4. `warehouse/stock-list.html` … `sales-list.html` と同じ絞り込みフォーム

**既存（変更不要・動いている）:** `POST /warehouse/stock/{id}/status` ステータス更新

### 注意（リポジトリ）

`ShopOrderRepository` に warehouse 絞り込み用メソッドを **書きかけてコンパイルエラー** になっていた。
壊れた行は削除済み。末尾に TODO コメントあり。**3メソッドはまだ未追加。**

---

## 写し元 ↔ 写し先 対応表

| 店舗（済） | 倉庫（今回） |
|-----------|--------------|
| `ShopOrderService.findActiveByShopIdFiltered` | `WarehouseOrderService.findActiveByWarehouseIdFiltered` |
| `shopId` | `warehouseId` |
| `ShopOrderRepository.findByShopIdAndDeleteFlagAndOrderStatus...` | `findByWarehouseIdAndDeleteFlagAndOrderStatus...` |
| `ShopOrderRepository.findByShopId...OrderDateBetween...` | `findByWarehouseId...OrderDateBetween...` |
| `ShopOrderRepository.findByShopId...Status...Between...` | `findByWarehouseId...Status...Between...` |
| `ShopSalesController` GET `/shop/sales` | `WarehouseOrderController` GET `/warehouse/stock` |
| `shop/sales-list.html` の filters | `warehouse/stock-list.html` に filters 追加 |

期間の扱い（片側だけ日付入力可、`LocalDate`→`LocalDateTime`）は **ShopOrderService と完全に同じロジック**。

---

## 倉庫絞り込みの次（今日〜明日の残り）

| 優先 | 内容 |
|------|------|
| 1 | 倉庫 CSV（`ShopSalesController.downloadSalesCsv` の warehouse 版） |
| 2 | 倉庫 PDF（`ShopSalesPdfService` 流用、列は店舗名・商品名・発注数・ステータス・発注日時・更新日時） |
| 任意 | ステータス更新後 redirect で絞り込みクエリを維持 |

---

## 主要ファイル一覧（参照用）

```
src/main/java/com/example/stepflow/
  controller/ShopSalesController.java      … 店舗絞り込み・CSV・PDF（写し元）
  controller/WarehouseOrderController.java … 倉庫受注（今回 list を拡張）
  service/ShopOrderService.java            … findActiveByShopIdFiltered（写し元）
  service/WarehouseOrderService.java       … 今回メソッド追加
  service/ShopOrderExcelService.java       … A3テンプレ + A4取り込み
  service/ShopSalesPdfService.java         … PDF生成
  repository/ShopOrderRepository.java      … 今回 warehouse 用3メソッド追加
  dto/ShopOrderImportResult.java           … A4結果

src/main/resources/templates/
  shop/sales-list.html                     … 絞り込みUI（写し元）
  warehouse/stock-list.html                … 今回フォーム追加
  shop/order-import.html                   … A4画面

scripts/  … dev-check.bat, run-app.bat 等
document/ … 仕様（お手本 Excel/PDF）、stepflow_schema.sql
```

---

## 動作確認チェックリスト（倉庫絞り込み後）

- [ ] `mvn clean compile` 成功
- [ ] 倉庫ユーザーで `/warehouse/stock` 表示
- [ ] ステータス「準備中」だけで件数が減る
- [ ] 開始日・終了日で絞れる
- [ ] 条件クリア（すべて）で全件に戻る
- [ ] 行のステータス更新（POST）が従来どおり動く

---

## 今回の依頼（新チャットで最初にやってほしいこと）

1. 上記「倉庫絞り込み」を **既存コードの書き方に合わせた模写コード全文**（**行ごとの日本語コメント付き**）でチャットに出す
2. **作業手順**（1→2→3→4の順）を短く
3. 写し終えたら私が「レビューして」と言うので、その前提で進める
4. コンパイルでよくあるミス（Repository メソッド名 typo 等）を注意点に書く

---

## 私の作業スタイル（覚えておいて）

- ガイドを見ながら **自分で打つ**
- わからない行は「この行がわからない」と聞く
- エラー時は `dev-check.bat` と IDE の赤線を共有する
- コミット・PR は自分から頼むまでしない

---

（以上・引き継ぎプロンプト終わり）
