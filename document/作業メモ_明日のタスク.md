# stepFlow 作業メモ（状況整理・明日やること）

*作成: 2026-05-26 時点の会話まとめ*

---

## いまの状況（進捗 約94〜95%）

### 完了しているもの

| 領域 | URL・メモ |
|------|-----------|
| 共通 | ログイン `/login`、ホーム `/home`、サイドバー、403 |
| 管理者 | ユーザー・倉庫・店舗・連携・カテゴリ・商品・`/admin/inquiries` |
| 店舗在庫 | `/shop/inventory` |
| 倉庫在庫 | `/warehouse/inventory` |
| 店舗発注 | `/shop/order`（登録時ステータス「準備中」） |
| 倉庫受注 | `/warehouse/stock` |
| 連携倉庫 | `/shop/relations`、問い合わせへ `?warehouseId=` リンク済 |
| お問い合わせ（共通） | `/inquiry` 一覧・絞り込み・新規・詳細・ステータス・削除・権限列 |
| 新規お問い合わせ | 連絡先は送信先で店舗/倉庫フィルタ（`shopPartnerList` / `warehousePartnerList` + JS） |
| 店舗ダッシュボード | `/home`（店舗のみ）発注ランキング・自店舗/全店舗・過去1か月 |
| 集計 | `daily_order_summary`（発注登録時に加算、`saveAndFlush` 連携済） |

`mvnw clean compile` … **成功**

### まだないもの（ロードマップ上の残り）

| 優先 | 内容 |
|------|------|
| **1** | **売上** `/shop/sales`（サイドバーリンクのみ・Controller/DB/画面なし） |
| 任意 | `prefecture` / `authority` マスタ（ERにはあるが実装なし・今回必須ではない） |

### 今回やらないこと

- 本格的なテスト工程（テスト仕様書・自動テストなど）
- 開発しながらの軽い動作確認（登録→一覧に出る）は可

### 使える時間

- **3日間で合計 約30時間**（1日 約10時間想定）

---

## 明日（1日目）のタスク ― 売上の土台

**ゴール:** Excel 確定 → DB 作成 → Entity / Repository / Service まで → `compile` 成功

| 順 | 時間目安 | やること |
|----|----------|----------|
| 1 | 2h | **Excel（お手本）で売上を確認** … テーブル名・列名・画面遷移・削除の要否をメモ |
| 2 | 1h | **A5:SQL** で売上テーブル `CREATE` + FK（`document/stepflow_schema.sql` に追記しても可） |
| 3 | 2h | **Entity** + **Repository**（模写元: `ShopOrder` / `ShopOrderRepository`） |
| 4 | 3h | **Service** … 自店舗 `shop_id` のみ登録・一覧（模写元: `ShopOrderService`） |
| 5 | 2h | `mvnw clean compile` … エラーが出たらその日のうちに修正 |

### 明日やらないこと（2日目以降）

- Controller / HTML（一覧・入力画面）
- `prefecture` / `authority` テーブル

### コピー元（最短ルート）

```
ShopOrder          → 売上 Entity
ShopOrderRepository
ShopOrderService
ShopOrderController  （2日目）
shop/order-list.html （2日目）
shop/order-form.html
```

URL だけ `/shop/sales`、テーブル名は **Excel 優先**。

---

## 2日目・3日目（参考・全体30h）

### 2日目（10h）― 画面まで

- `ShopSalesController`（GET 一覧、GET/POST 新規）
- `shop/sales-list.html` / `shop/sales-form.html`
- 店舗ログインで登録→一覧（軽く1回確認）

### 3日目（10h）― 仕上げ

- Excel と突合（不足列・バリデーション）
- `document/引き継ぎプロンプト_2026-05-25.md` 更新（進捗・売上URL）
- デモ用操作順を1枚メモ
- コンパイル・画面の細かい修正

---

## 売上 DB がまだ Excel で見えないとき

お手本 ER の `order` 近辺を確認。なければ講師・チームに **テーブル名と列** だけ先に確認する（1日目の最初の2hが最重要）。

---

## ダッシュボード関連（済・参照用）

- 過去発注だけあってランキングが空 → 下記 SQL で `daily_order_summary` を埋める（任意）

```sql
INSERT INTO daily_order_summary (count_date, shop_id, goods_id, goods_amount)
SELECT DATE(order_date), shop_id, goods_id, SUM(order_quantity)
FROM shop_order WHERE delete_flag = 0
GROUP BY DATE(order_date), shop_id, goods_id
ON DUPLICATE KEY UPDATE goods_amount = VALUES(goods_amount);
```

---

## 新チャット用（明日続き）

```
document/作業メモ_明日のタスク.md を読んでください。
売上 /shop/sales の Entity・Repository・Service から（または Excel 確認から）続きをお願いします。
```

---

*関連: `document/引き継ぎプロンプト_2026-05-25.md`（全体引き継ぎ）*
