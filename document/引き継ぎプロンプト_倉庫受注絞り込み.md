# stepFlow 引き継ぎプロンプト（倉庫受注 絞り込み）

新チャット開始時に **この全文を貼る** + 必要なら `document/` 配下（Excel・PDF）を添付。

---

## あなた（AI）へのお願い

- 私は **自分でコードを打ちたい**。基本は **手順・模写コード（詳しいコメント付き）・レビュー**。
- 詰まった・コンパイルエラー・動かないときだけ「修正して」と言うので、そのときはローカル修正してよい。
- 仕様の正は **`document/` 配下**（チーム開発ロードマップ（お手本））。食い違いは Excel/PDF 優先。

---

## プロジェクト

| 項目 | 値 |
|------|-----|
| パス | `C:\Users\owner\ForDevelop\stepflow\stepflow` |
| 技術 | Spring Boot 3.4.3 / Java 17 / MySQL / JPA / Thymeleaf / Security / POI / OpenPDF |
| ビルド | `.\mvnw.cmd clean compile` |

---

## 今回やること（最優先）

**倉庫：受注一覧の絞り込み**（店舗の `/shop/sales` と同型を倉庫に写す）

| 項目 | 内容 |
|------|------|
| URL | `GET /warehouse/stock` |
| 画面 | `warehouse/stock-list.html` |
| 条件 | ステータス（準備中 / 発注済）、開始日 `from`、終了日 `to` |
| 対象 | ログイン倉庫（`user.warehouse_id`）への受注のみ |

**店舗側は完了済み（写し元）:**

- `ShopOrderService.findActiveByShopIdFiltered`
- `ShopSalesController` + `shop/sales-list.html`（絞り込みフォーム）
- `ShopOrderRepository` の `findByShopId...Between...` 系3メソッド

---

## 未実装・これから写すファイル

| 順 | ファイル | 作業 |
|----|----------|------|
| 1 | `ShopOrderRepository.java` | warehouse 用メソッド3つ追加 |
| 2 | `WarehouseOrderService.java` | `findActiveByWarehouseIdFiltered` 追加 |
| 3 | `WarehouseOrderController.java` | `list` に `status/from/to` パラメータ |
| 4 | `warehouse/stock-list.html` | 絞り込みフォーム（`sales-list.html` 写し） |

**注意:** `ShopOrderRepository` に未完成の1行を書きかけてコンパイルが落ちていた。壊れた行は削除済み。TODO コメントあり。

---

## 写し元 ↔ 写し先 対応表

| 店舗（済） | 倉庫（今回） |
|-----------|--------------|
| `findActiveByShopIdFiltered` | `findActiveByWarehouseIdFiltered` |
| `shopId` | `warehouseId` |
| `findByShopIdAndDeleteFlagAndOrderStatus...` | `findByWarehouseIdAndDeleteFlagAndOrderStatus...` |
| `ShopSalesController` GET `/shop/sales` | `WarehouseOrderController` GET `/warehouse/stock` |
| `sales-list.html` filters | `stock-list.html` filters |

期間変換ロジック（`LocalDate` → `LocalDateTime`、片側だけ入力可）は **ShopOrderService と同じ**。

---

## 店舗側 完了機能（触らない・参考のみ）

- `/shop/order` 商品選択・1件発注・一覧
- `/shop/order/template/download` Excelテンプレ
- `/shop/order/import` Excel一括発注（A4）
- `/shop/sales` 発注履歴 + 絞り込み + CSV + PDF
- デモバッチ（5分ごとユーザー追加）+ `scripts/*.bat`（学習用）

---

## このあと（倉庫絞り込みの次）

| 優先 | 内容 |
|------|------|
| 次 | 倉庫 CSV / PDF（`ShopSalesController` の csv/pdf を warehouse 版に） |
| 任意 | ステータス更新後も絞り込み条件を維持（redirect にクエリ付与） |

---

## 動作確認

1. 倉庫ユーザーでログイン（`warehouse_id` 設定済み）
2. `/warehouse/stock` → 絞り込みフォーム表示
3. ステータス・期間で件数が変わる
4. ステータス更新（POST）が従来どおり動く
5. `mvn clean compile` 成功

---

## 新チャットで最初に送る文（コピペ用）

```
document/引き継ぎプロンプト_倉庫受注絞り込み.md を読んでください。

次は倉庫の受注一覧 /warehouse/stock の絞り込み（ステータス・期間）です。
店舗の /shop/sales と同じパターンで、模写コードと詳しいコメント付きの手順を教えてください。
まだ自分で写していないので、既存コードの書き方に合わせた全文をチャットに出してください。
写し終えたらレビューします。
```

---

*作成: 倉庫絞り込み着手前 / 店舗 A4・sales・PDF 完了後*
