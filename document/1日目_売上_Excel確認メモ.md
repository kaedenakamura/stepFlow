# 1日目メモ：売上（/shop/sales）Excel 確認結果

*2026-05-27 作業開始時*

---

## ステップ1 結論（Excel 確認）

お手本 Excel（`チーム開発ロードマップ（お手本）.xlsx`）を確認しました。

### 売上テーブルは **定義されていない**

| Excel にあるもの | 内容 |
|------------------|------|
| `order` / `shop_order` | 店舗→倉庫への **発注**（既に `/shop/order` で実装済み） |
| `daily_order_summary` | 発注ランキング用集計（ダッシュボードで実装済み） |
| 店舗機能 | ダッシュボード、在庫、連携倉庫、**発注管理**、発注履歴（CSV/PDF） |
| **売上 / shop_sales** | **機能一覧・テーブル設計に記載なし** |

サイドバーの「売上入力・確認」(`/shop/sales`) は **自前で追加したリンク** の可能性が高いです。

### チームで決めること（5分でOK）

次のどちらかを選ぶ（講師に聞けるなら確認）:

| 案 | 内容 | 1日目の作業 |
|----|------|-------------|
| **A. 店舗売上テーブル新規** | 自店の「販売数量」を記録（倉庫発注とは別） | 下記 `shop_sales` で進める |
| **B. 発注履歴の別URL** | Excel の「発注履歴確認」を `/shop/sales` に寄せる | 新テーブル不要。2日目に Controller だけ |

**おすすめ:** 残り3日・売上入力画面名なら **案A（shop_sales 新規）** がサイドバーと一致しやすい。

---

## 案A で進める場合のテーブル案（Excel の order を簡略化）

倉庫は不要（店舗で売れた数だけ記録）。

| 列 | 型 | 意味 |
|----|-----|------|
| shop_sales_id | INT PK | 売上ID |
| shop_id | INT FK | 自店舗 |
| goods_id | INT FK | 商品 |
| sales_quantity | INT | 販売数量（1以上） |
| sales_date | TIMESTAMP | 登録日時（DB DEFAULT） |
| delete_flag | INT | 0=有効 |

画面（2日目）: 一覧 + 新規入力（商品・数量）。自店舗のみ。

---

## 今日（1日目）のゴール

- [ ] 上記 A/B を決める
- [ ] A5 で `shop_sales` 作成（案Aの場合）
- [ ] Entity / Repository / Service まで
- [ ] `mvnw clean compile` 成功
- [ ] Controller / HTML は **明日**

---

*続き: 下記コードを手打ち（解説付きはチャット参照）*
