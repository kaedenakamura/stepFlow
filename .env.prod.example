# 本番用環境変数テンプレート
# 使い方: このファイルを .env.prod にコピーし、値を変更してから compose を実行

# アプリ公開ポート（サーバー IP:9138 でアクセス）
APP_PORT=9138

# MySQL（docker/init の初期 SQL とセットで使う）
MYSQL_ROOT_PASSWORD=change-me-root
MYSQL_USER=stepflow
MYSQL_PASSWORD=change-me-app
