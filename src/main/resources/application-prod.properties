# 本番プロファイル（SPRING_PROFILES_ACTIVE=prod）
# docker-compose.prod.yml から DB 接続は環境変数で上書き

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# 学習用デモバッチは本番では常に OFF（存在しないパスを指定）
stepflow.demo-batch.flag-file=/tmp/demo-batch.OFF
